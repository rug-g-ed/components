This is a component library using vanilla TypeScript and html with vite as the build tool.

# Repository Structure
## Components
Each component has a separate folder under src/components. This is where the css typescript files for the component are placed.

# Code Style
- Always use strong types
- Never use the any type